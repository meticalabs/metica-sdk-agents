// Generated by metica-sdk-agents (side-by-side codegen).
// Interstitial-only MeticaSdk wrapper. Owned by MeticaAdProvider, which
// re-raises these events through IAdService.
//
// Lifecycle: ctor is pure. SubscribeCallbacks() runs from
// MeticaAdProvider.Initialize() before MeticaSdk.Initialize. Load/Show/IsReady
// are called by user code via MeticaAdProvider → IAdService after init.
using System;
using UnityEngine;
using Metica;
using Metica.Ads;

namespace Metica.AbTest
{

public class MeticaInterstitialProvider
{
    private const string SDK_SOURCE = "MeticaSdk";

    public event Action<AdEventData> OnLoaded;
    public event Action<AdErrorData> OnLoadFailed;
    public event Action<AdEventData> OnShown;
    public event Action<AdEventData> OnHidden;
    public event Action<AdEventData> OnRevenuePaid;

    private bool _subscribed;

    public void SubscribeCallbacks()
    {
        if (_subscribed) return;
        _subscribed = true;

        // Fires when an interstitial ad has been loaded and is ready to show.
        MeticaAdsCallbacks.Interstitial.OnAdLoadSuccess  += ad  => OnLoaded?.Invoke(ToEventData(ad));
        // Fires when an interstitial ad failed to load (no fill, network, etc.).
        MeticaAdsCallbacks.Interstitial.OnAdLoadFailed   += err => OnLoadFailed?.Invoke(ToErrorData(err));
        // Fires when the interstitial ad has been displayed to the user.
        MeticaAdsCallbacks.Interstitial.OnAdShowSuccess  += ad  => OnShown?.Invoke(ToEventData(ad));
        // Fires when the user dismisses the interstitial. Resume gameplay here.
        MeticaAdsCallbacks.Interstitial.OnAdHidden       += ad  => OnHidden?.Invoke(ToEventData(ad));
        // Fires when impression revenue is reported for an interstitial. Forward
        // to your attribution/analytics pipeline.
        MeticaAdsCallbacks.Interstitial.OnAdRevenuePaid  += ad  => OnRevenuePaid?.Invoke(ToEventData(ad));
    }

    public void Load(string adUnitId)    => MeticaSdk.Ads.LoadInterstitial(adUnitId);
    public bool IsReady(string adUnitId) => MeticaSdk.Ads.IsInterstitialReady(adUnitId);
    public void Show(string adUnitId, string placement = null, string customData = null)
        => MeticaSdk.Ads.ShowInterstitial(adUnitId, placement, customData);

    private static AdEventData ToEventData(MeticaAd ad) => new AdEventData
    {
        AdUnitId    = ad.adUnitId,
        NetworkName = ad.networkName,
        Revenue     = ad.revenue ?? 0,
        AdFormat    = ad.adFormat,
        Placement   = ad.placementTag,
        LatencyMs   = ad.latency ?? 0,
        SdkSource   = SDK_SOURCE
    };

    private static AdErrorData ToErrorData(MeticaAdError err) => new AdErrorData
    {
        AdUnitId  = err.adUnitId,
        Message   = err.message,
        SdkSource = SDK_SOURCE
    };
}

} // namespace Metica.AbTest
